import React from 'react';

import Layout from '../components/Layout/Layout';

function IndexPages({page:page,}) {
  return (
      <Layout>
   {{page}}
      </Layout>
  
  );
}

export default IndexPages;
